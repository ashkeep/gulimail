spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/guli_wms?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: 123456
#  profiles:
#    active: dev

mybatis-plus:
  mapper-locations: classpath:/mapper/wms/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 13000


  #指定不同目录下的日志的级别
# 打印sql
#logging:
#  level:
#    com.atguigu.gulimall.wms.dao : debug
